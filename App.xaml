<Application x:Class="SpaceEditor.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:SpaceEditor"
             xmlns:adonisUi="clr-namespace:AdonisUI;assembly=AdonisUI"
             xmlns:adonisExtensions="clr-namespace:AdonisUI.Extensions;assembly=AdonisUI"
             xmlns:adonisConverters="clr-namespace:AdonisUI.Converters;assembly=AdonisUI"
             xmlns:adonisControls="clr-namespace:AdonisUI.Controls;assembly=AdonisUI"
             StartupUri="MainWindow.xaml"
             Exit="OnExit">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/AdonisUI;component/ColorSchemes/Dark.xaml"/>
                <ResourceDictionary Source="pack://application:,,,/AdonisUI.ClassicTheme;component/Resources.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        
            <Style x:Key="{x:Type GridSplitter}" TargetType="GridSplitter" BasedOn="{StaticResource {x:Type GridSplitter}}">
                <Setter Property="ResizeBehavior" Value="PreviousAndNext" />
                <Setter Property="Background" Value="#FF7F7F7F"/>
                <Setter Property="Width" Value="5"/>

                <!-- TODO: https://github.com/benruehl/adonis-ui/issues/212 -->
                <Setter Property="ShowsPreview" Value="True" /> 
            </Style>

            <!--<Style TargetType="ScrollViewer" BasedOn="{StaticResource {x:Type ScrollViewer}}">
                <Setter Property="Margin" Value="0"/>
            </Style>-->


            <!--<Style x:Key="{x:Type ScrollViewer}" TargetType="ScrollViewer" BasedOn="{StaticResource {x:Type ScrollViewer}}">
                <Setter Property="Background" Value="White"></Setter>

            </Style>

            <Style x:Key="{x:Type GroupBox}" TargetType="GroupBox" BasedOn="{StaticResource {x:Type GroupBox}}">
                <Setter Property="Background" Value="White"></Setter>
            </Style>-->
        </ResourceDictionary>
    </Application.Resources>
</Application>
