<UserControl x:Class="SpaceEditor.Controls.PCUUnlocker"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SpaceEditor.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Image
            VerticalAlignment="Bottom"
            HorizontalAlignment="Right"
            Opacity="0.4"
            Stretch="Uniform"
            Margin="2, 0, 0, 0"
            Source="pack://application:,,,/Resources/SpaceEditor.png"
            />

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <TextBlock
                Margin="2"
                Text="Will make your PCU pool so high, that you won't be able to exhaust it even if you try :)"
                />
                
            <Button
                Grid.Row="1"
                Margin="2, 2, 0, 0"
                Content="Unlock my PCU!"
                Click="UnlockPCU"
                />
        </Grid>
    </Grid>
</UserControl>
